Этот файл создан для меня лично, нужен, чтобы я отслеживал по хронологии ход своих мыслей, открытия, на пути и т.д.

21.08
ранее созданная верстка, обернутая в TS, за несколько часов ожила. Теперь создаются в редаксе контакты, редактируются, удаляются по одиночке и скопом. В процессе поиск, промахи в верстке. После этого приступлю к реализации аутенфикации
на данный момент имеется страница  Home без реализованного поиска по контактам, создан слайс по контактам, там 4 слайса - добавление, изменение, удаление конкретного, удаление всех контактов.
инпут поиска был вынесен в отдельный компонент, но не доделан. контакт тоже вынесен в отдельный копонент.


22.08
в тщетных попытках реализовать поиск на базе редакса, было принято решение, отложить реализацию поиска, пока не будет подключен БЭК
Идея поиска заключалась в том, чтобы взять за основу реализацию с РеактПиццы 2, но без БЭКа. Однако, при реализации фильтрации массива с редакса и создания нового, отфильтрованного массива редакса начались проблемы. Одной из них было, что если добавить в зависимости useEffect'a оба массива(исходный и отфильтрованный), то он не производил перерисовку. Короче я ничего не понял, как и почему там не заработало.
Предстоит: 
- подключить API
- разобраться и впервые сделать аутенфикацию пользователя
- реализовать поиск от бэка, через debounce.lodash
- если реализуется норм бэк, то убрать nextContactIdx, т.к. айди будет присваиваться бэкендом
- сделать добавление записи и редактирование через форму. чтобы инпут был контролируемым, а диспатч происходит по submit'у 
- прибраться в компонентах (вынести из Home функции-обработчики кнопок)
- добить некорректные моменты по верстке
- реализовать модульный подход в хранении css

23.08
изучал бэк

05.09 
добиваю API
- убрал кнопку удаления всего, потому что бред - станет пустой файл и придётся всё самому добавлять для просмотра, а мне нравится изначальный список контактов))
